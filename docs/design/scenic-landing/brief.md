# Foundkeep scenic landing

The user supplied a precise reference: an azure mountain hero, white space, rounded sky-blue and mint product panels, small glass controls, a scenic closing banner and footer. Build that direction directly. Preserve the Foundkeep mark, real capture and collection behavior, account and store links, support, privacy and manual installation. iPhone is in TestFlight; do not advertise public App Store availability, Android/native desktop apps, invented pricing or customer metrics.

This change owns the marketing landing page and its assets only. Mobile retains the approved deep-purple gallery and floating dock. Auth, dashboard and support routes retain their current functional styles.

Visitor path: scenic promise and primary create-account action → live highlight capture demonstration → actual library screenshot and source retention → browser, iPhone beta, web and organization panels → local/cloud choices and getting started → FAQ → scenic closing CTA and footer.

Interactions: working highlight save/reset, accessible mobile navigation, native details FAQ, anchor navigation. Motion: a short scenic arrival animation and spring-like hover response, disabled by reduced motion. All useful content is visible without JavaScript. Render desktop 1440px, tablet 768px and phone 390px/320px; verify links, assets, overflow, keyboard and capture demo. Source current stable product screenshots; generate an original scenic image and store responsive WebP versions locally.
