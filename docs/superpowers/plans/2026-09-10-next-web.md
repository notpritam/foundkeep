# Foundkeep Next.js Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for the bounded independent auth/dashboard tasks; root implements framework, public pages and deployment.

**Goal:** Full-viewport auth with session redirects and a real optimized React/Next customer web.
**Architecture:** Next static public routes, dynamic private server pages, React client islands and account-scoped TanStack Query; existing Bun API unchanged.
**Tech Stack:** Next 16.3.4, React 19.3.0, TypeScript, TanStack Query 5.102.8, Bun/Hono.
**Spec:** docs/superpowers/specs/2026-09-10-next-web-design.md

## Global constraints
- Preserve every existing capture, settings, recovery, deletion, OAuth, source and device pairing flow.
- No Supabase secrets/client SDK; no shared mutable server user cache.
- Scenic web design remains; app/extension package versions remain unchanged.
- Existing authorization covers implementation, tests, push and live deployment.

## Work and interfaces

- [x] Root: scaffold apps/site, dependencies, common API/session/config helpers, scoped CSS, public React pages, metadata, old URL redirects, API passthrough and CSP. Build with `bun run --cwd apps/site build` and validate actual static/dynamic output.
- [x] Auth implementer: React full-viewport auth, forms, account recovery/export confirmation, OAuth provider start/completion/link/deletion. Routes /login,/signup,/recover,/auth. Server helper `getSession(): Promise<Me|null>` redirects ordinary logged-in visits; handoff parameters are exceptions. Client helper `api<T>(path, {method,body,signal,accountId,download})` retains backend errors and account guards. Preserve all input controls and review every legacy auth.js/oauth.js branch. Add integration cases through root's tests/next-web.mjs.
- [x] Dashboard implementer: `app/dashboard/page.tsx`, `components/dashboard/*`, URL parser and client query provider. Server `getSession()` and `serverApi<T>(path, accountId?)` supply first response; `api<T>` receives account ID on all account-sensitive client calls. Implement actual React components, not HTML injection or legacy script execution. Lazy dialogs, data loaders, finite safe URL state, realtime refresh, device suggestions, settings, recovery, deletion, capture reads/files and export. Preserve native confirmation/focus behavior and all customer.js safety functions.
- [x] Root: migrate landing/footer/support/policies/handoff with genuine React islands for install detection, beta configuration, mobile menu and demo. Assets remain self-hosted with sizes/preload and cache policy; three generated landscape variants optimized to self-hosted WebP.
- [x] Verification: production Next build; backend regressions; browser signup/login/logout/recovery/deletion, SSR guards, stale session/cache separation, query back/forward, capture and device settings, OAuth handoff errors, .html redirects, iPhone link, real extension pairing, public route no-JS HTML and responsive screenshots. Use independent code and visual review before release.
- [x] Deployment: install loopback Next service on 8791; verify direct healthy production build; route only web to Next in scoped Foundkeep Caddy block; keep backend/API routes at8790. Check live auth redirects without JavaScript and signed-in browser flow; record actual bundle/rendering results and rollback command.

## Execution ledger
- 2026-09-10: production and worktree at7b65014 before migration. Production has unrelated docs/SUPABASE-AUTH-SETUP.md and docs/auth-verification/ changes; preserve.
- Ruling: proceed under explicit user request rather than re-asking approval gates in generic skills. The work is architectural; user chose React/Next and requested optimization end to end.
- Ruling: delegate auth and dashboard to independent focused implementers under this skill; root owns integration and public pages. No concurrent edits of shared helpers/root package files.

- Released085d4f2 to main and production. Full live read-only browser/HTTP checks passed; database counts preserved. Evidence: docs/design/next-web/verification.md.
