[Service]
# Mode-0600 file outside the checkout. Never add these values to Expo, web,
# extension configuration, public runtime policy, or build-time variables.
EnvironmentFile=/home/pritam/.config/foundkeep/supabase.env
