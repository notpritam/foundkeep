node_modules/
dist/
build/
*.log
.env
.env.*
!.env.example
.DS_Store

# Runtime data (SQLite db + blobs) lives on omni, never committed
apps/backend/data/

# Extension build output
apps/extension/dist/

# extension signing key (keep secret!)
deploy/keys/

# stray crx3 default-name output
web-extension.*

# Synthetic browser review screenshots
.impeccable/review/

# Compiled native-app browser verification
apps/mobile/dist-gallery-preview/
