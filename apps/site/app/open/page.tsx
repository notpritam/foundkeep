import Link from 'next/link';
import type {Metadata} from 'next';
import {AppAccessLink} from '@/components/iphone-access';
export const dynamic='force-dynamic';
export const metadata:Metadata={title:'Open Foundkeep',robots:{index:false,follow:false}};
export default async function Open({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams;const raw=typeof query.path==='string'?query.path:'collection';const path=/^(login|register|recover|collection|settings|new-note|capture\/[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/i.test(raw)?raw:'collection';const browser=path==='login'?'/login':path==='register'?'/signup':path==='recover'?'/recover':'/dashboard';return <div className="customer-body open-body"><header className="site-header privacy-header wrap"><Link className="brand" href="/"><img src="/assets/studio-mark.svg" width="36" height="36" alt=""/><span>Foundkeep</span></Link><Link className="text-link" href="/support">Help</Link></header><main className="open-page"><section className="open-card"><div className="open-mark"><img src="/assets/studio-mark.svg" width="72" height="72" alt=""/></div><h1>Continue to your collection.</h1><p>Open your library in Foundkeep for iPhone. Sign in with the same account to bring your saves together.</p><div className="open-actions"><a className="button primary" id="open-app" href={`foundkeep://${path}`}>Open Foundkeep</a><Link className="text-link" id="open-browser" href={browser}>Continue in the browser</Link></div><p className="open-private">Don’t have the app yet? <AppAccessLink/>.</p></section></main></div>;}
